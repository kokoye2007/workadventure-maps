# WorkAdventure Maps Configuration

# Upload mode for deployment
# Options: GH_PAGES (GitHub Pages) or MAP_STORAGE (WorkAdventure hosting)
UPLOAD_MODE=GH_PAGES

# Map Storage Configuration (only needed if UPLOAD_MODE=MAP_STORAGE)
# MAP_STORAGE_URL=your-storage-url
# MAP_STORAGE_API_KEY=your-api-key
# UPLOAD_DIRECTORY=your-upload-directory

# Development Configuration
VITE_PORT=5173